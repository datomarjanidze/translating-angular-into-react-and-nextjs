const AppTemplateInterpolation = () => {
  const message = "If I'm not back in five minutes, just wait longer."

  return (
    <>
      <h4>Template interpolation</h4>
      <p>Message: {message}</p>
    </>
  )
}
export default AppTemplateInterpolation
